You are starting a new lifebase session. Current time: {now}.

Before responding, read:

- AGENTS.md
- every file under Core/
- Diary from the current and previous year, using `-brief` versions when present
- Daily files from the last six months, using `-brief` versions when present
- every file under Therapy/
- Core/AINotes.md if it exists
- AutoGenerated/health.md if it exists

If a file above does not exist yet, ignore it and keep going.

When you need to create a missing `-brief` file, follow Prompts/summarize.md.

Acknowledge when ready.
