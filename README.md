# lifebase
Command-line tool and daemon for your LifeBase, a comprehensive database about yourself.

Run `lifebase -init` inside an empty directory or fresh Git repo to create a ready-to-use example lifebase. Existing files are left alone, except `.gitignore`, which is merged with the required ignore lines.
